[package]
name = "shell"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
errno = "0.3.14"
nix = { version = "0.31.3", features = ["process", "signal"] }
